%% Header that the buildfile includes into the pandoc standard header. Use this to customize options that aren't already set in the layout.

% packages
\usepackage{color}
\usepackage{geometry}
\usepackage[T1]{fontenc}
\usepackage{titlesec}
\usepackage[T1]{fontenc}

% page layout
\geometry{top=3cm, left=3cm, right=3cm}

% page header
\usepackage{fancyhdr}
\pagestyle{fancy}
\lhead{TheAlternative}
\rhead{Hacking Session - \input{git_tag}}
\cfoot{\thepage}
\renewcommand{\headrulewidth}{0.4pt}
\renewcommand{\footrulewidth}{0.4pt}

% set custom layout for chapters. Search for `titlesec package` for
% the documentation (or open http://mirror.utexas.edu/ctan/macros/latex/contrib/titlesec/titlesec.pdf)
\titleformat{\chapter}[hang]
  {\sffamily\bfseries\huge}%
    {\thechapter\ }
	    {0 pt}%
	      {}

% Define colors for listings
\definecolor{mygreen}{rgb}{0,0.6,0}
\definecolor{mygray}{rgb}{0.5,0.5,0.5}
\definecolor{mymauve}{rgb}{0.58,0,0.82}

% Define styles for listings
\lstset{%
	basicstyle=\ttfamily,
	}
\lstdefinestyle{customstyle}{%
	numbers=none,
	language=bash,
	showspaces=false,
	showstringspaces=false,
	commentstyle=\color{mygreen},
	breaklines=true,
	breakatwhitespace=true,
	keywordstyle=\color{blue},
	morekeywords={wc,mkdir,rm,ln,sudo,su,chown,diff,touch,grep,file,less,which,whereis,ls,mv,cd,cat,cp,clear,chmod,which,},
	}

% Redefine layouts used by pandoc for listings.
% Redefining these environments took me two hours. If someone knows a good insurance company for RSI, hit me up.
\let\oldlisting\lstlisting
\let\endoldlisting\endlstlisting
\renewenvironment{lstlisting}{\oldlisting[style=customstyle]}{\endoldlisting}
